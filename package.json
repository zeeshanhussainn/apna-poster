{
  "name": "apna-poster",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "cd Client && npm install && npm run build && move dist ..",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "dotenv": "^17.2.3",
    "cors": "^2.8.5",
    "razorpay": "^2.9.6"
  },
  "devDependencies": {
    "supabase": "^2.47.2"
  }
}